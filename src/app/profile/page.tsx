import { getMetadata } from "@/app/sharedMetadata";

export { default } from '@/containers/profile';

export const metadata = getMetadata({
  title: 'CHIMSIL',
  description: '프론트엔드 개발자로 일 하고 있는 chimsil의 작업 목록',
});